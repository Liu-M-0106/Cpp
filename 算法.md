## 时间复杂度
定义：用于描述算法执行时间随输入规模增长的变化趋势
常见时间复杂度排序：
+ O(1):常数时间——访问数组元素
+ O($log n$):对数时间——二分查找
+ O($n$):线性时间——遍历数组
+ O($n*log n$):线性对数时间——快排、归并排序
+ O($ n^2 $):平方时间——冒泡排序、插入排序
+ O($2^n$):指数时间——递归求解斐波那契数列

## [二分查找](Array\BinarySearch.cpp)
使用条件：有序数组+无重复元素
不需要额外的空间，速度极快`logn`
注意边界条件，防止溢出：左边界`if (mid == 0 )`，右边界`if (mid == nums.size()-1)`(闭区间)
半开区间求得的右边界是第一个大于target的边界

##### 1.T34[寻找左右边界](Array\T34.cpp)
个人题解中判断左右边界占用内存高，多次读取num.size()造成额外开销
**参考标准题解：两次半开区间的二分实现简洁高效计算**
最终输出结果`if`部分：左边界值小于数组长度并验证等于target


##### 2.T69[x的平方根](Array\T69.cpp)
注意二分过程中左右边界作为开区间或闭区间
在计算平方过程中需要用`long long `防止溢出
